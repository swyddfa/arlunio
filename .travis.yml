language: python
python:
    - 3.5
    - 3.6
    - 3.7

before_install:
    - "pip install tox coveralls poetry"

install:
    - "poetry install"

jobs:
    include:
        - stage: "Test"
          script: poetry run pytest --cov=stylo -q

notifications:
    webhooks:
        urls:
        - https://webhooks.gitter.im/e/4bfa4fbff35ebee0a9d2
