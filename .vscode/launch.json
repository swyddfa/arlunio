{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "python",
            "request": "launch",
            "name": "Pytest: Current File",
            "program": "${workspaceFolder}/.env/bin/pytest",
            "args": [
                "${file}"
            ]
        }
    ]
}